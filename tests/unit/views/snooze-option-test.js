import { moduleFor, test } from 'ember-qunit';

moduleFor('view:snooze-option', 'Unit | View | snooze option');

// Replace this with your real tests.
test('it exists', function(assert) {
  let view = this.subject();
  assert.ok(view);
});
